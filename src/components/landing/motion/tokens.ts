/** Shared motion tokens — keep all timing consistent across the page. */
export const MOTION_EASE = [0.22, 1, 0.36, 1] as const;
export const MOTION_DURATION = {
  fast: 0.35,
  base: 0.6,
  slow: 0.9,
} as const;
