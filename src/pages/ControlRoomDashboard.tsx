// Redirect removed – /control-dashboard is now served by /control-room
// Component kept as empty stub to avoid breaking any remaining imports
export default function ControlRoomDashboard() {
  return null;
}