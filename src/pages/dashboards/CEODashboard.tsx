/**
 * CEODashboard — alias of the unified Executive Cockpit.
 * Kept as a separate file so existing sidebar/registry routes
 * (`ceo-dashboard` and similar) keep resolving without changes.
 */
export { default } from "@/pages/ExecutiveDashboard";
